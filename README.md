# deep-navy-site

Jekyll public site and authenticated customer application shell. Public content
is rendered statically. The `/app/` shell authenticates through the API's
GitHub-to-Cognito flow and uses generated ConnectRPC clients; it contains no
provider credentials.

